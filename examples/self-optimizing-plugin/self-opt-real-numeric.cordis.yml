# 真实运行组合（任务：numeric-function）。与 self-opt-real.cordis.yml 只差 solver/evaluator 两行。

- id: hmr
  name: '@deepseek-ai/cordis-plugin-hmr'
  config:
    root: ['.']

- id: solver
  name: './tasks/numeric-function/solver.ts'

- id: evaluator
  name: './tasks/numeric-function/evaluator.ts'

- id: optimizer
  name: './src/optimizer.ts'

- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../headless-agent/cordis.yml
